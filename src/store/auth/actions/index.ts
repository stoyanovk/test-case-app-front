import {
  fetchLogin,
  login,
  logout,
  fetchRegister,
  fetchConfirmRegister,
  getAuthUser,
  setMessage,
  setError,
} from "./actions";
export {
  fetchLogin,
  login,
  logout,
  fetchRegister,
  fetchConfirmRegister,
  getAuthUser,
  setMessage,
  setError,
};
