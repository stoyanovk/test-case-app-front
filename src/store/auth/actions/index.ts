import {
  fetchLogin,
  login,
  logout,
  fetchRegister,
  fetchConfirmRegister,
  getAuthUser,
  setMessage,
  setError,
  fetchLogout,
} from "./actions";
export {
  fetchLogin,
  login,
  logout,
  fetchRegister,
  fetchConfirmRegister,
  getAuthUser,
  setMessage,
  setError,
  fetchLogout,
};
